# Base URL del backend (ASP.NET Core). En etapa de mocks puede quedar vacía.
NEXT_PUBLIC_API_BASE_URL=

# Indica si los servicios usan datos mock ("true") o el backend real ("false").
NEXT_PUBLIC_USE_MOCKS=true
